---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sundeeppahariya/spring-cloud-microservice
          searchPaths: apps-configurations-files
        # "native" is used when the native profile is active, for local tests with a classpath repo:
        native:
          searchLocations: classpath:offline-repository/
server:
  port: 8001
  